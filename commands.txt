# Docker commands to run the docker cellprofiler image with examples
# Pull the docker image from Docker Hub ore create is from the Dockerfile
#

docker build -t mydockercp2 .

docker run --rm -t -i cp d:/data/ dockercp:/tmp/data/mydockercp cellprofiler -c -p /tmp/data/ExampleHuman.cppipe -o /tmp/data/output -i /tmp/data

docker run -t mydockercp
ctrl-c
docker run -t mydockercp --name dockercp2

docker ps

docker rename 2e14f4ddce7f dockercp

docker cp d:/data/ dockercp:/tmp/data/

# mount a volume to the container
docker run -v /c/Users/data/:/data --name dockercp ls /data

docker exec dockercp /usr/cellprofiler/src/CellProfiler/cellprofiler -c -r -i /data/ -o /data/Output/ -p /data/ExampleTumors.cppipe

#docker exec dockercp2 cellprofiler -c -p /tmp/data/ExampleHuman.cppipe -o /tmp/data/output/

docker cp dockercp:/tmp/data/ d:/dataFromDocker2/

docker exec dockercp2 rm -r /tmp/data/

docker exec dockercp2 cellprofiler -c -p /tmp/data/ExampleHuman.cppipe -o /tmp/data/Output/ -i /tmp/data/







